meshConfig:
  outboundTrafficPolicy:
    mode: REGISTRY_ONLY
  enableTracing: true
  defaultConfig:
    tracing:
      sampling: 100.0
      zipkin:
        address: jaeger-collector.istio-system.svc.cluster.local:9411
